import { debug } from "vitest-preview";

/**
 * Use this to get a preview of the current HTML of your test. To view the output,
 * run `yarn run testPreview`, then run your tests (and reload your browser tab)
 */
export const showTestPreview = () => {
  debug();
};
