{
  "cancel": "Cancel",
  "errors": {
    "default": "Invalid value",
    "format": "Must follow the correct format",
    "invalidDate": "Invalid Date.",
    "maxDate": "Date should not be after {{max}}",
    "maxLength": "Must be shorter than {{max}} characters",
    "maxValue": "Must be less than or equal to {{max}}",
    "minDate": "Date should not be before {{min}}",
    "minLength": "Must be longer than {{min}} characters",
    "minValue": "Must be greater or equal than {{min}}",
    "mustBeNumber": "Must be a number",
    "oneOf": "Invalid value, must be one of: {{values}}",
    "positiveNumber": "Must be a positive number",
    "required": "Required"
  },
  "submit": "Submit"
}
